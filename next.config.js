/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'via.placeholder.com',
      'img.youtube.com',
      'images.pokemontcg.io',
    ],
  },
}

module.exports = nextConfig
